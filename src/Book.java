import javax.xml.namespace.QName;

public class Book {
    String name;
    String author;
    int year;

    public static void method(String name, String author, int year) {
        System.out.println(name + author + year);
    }
}
